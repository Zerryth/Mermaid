```mermaid
    classDiagram
        class TokenResponse {
        + string ChannelId
        + string ConnectionName
        + string Token
        + string Expiration
        + JObject Properties
        + CustomInit()
    }
```